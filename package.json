{
  "name": "cloud-itonami-kaisya",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "clojure -M:render-console",
    "dry-run": "npm run build && wrangler deploy --dry-run --outdir dist",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.120.1"
  }
}
