(function(global){'use strict';function hydrateSession(root,facts){if(!root)return null;facts=facts||{};const q=k=>root.querySelector('[data-ck-account="'+k+'"]');const name=String(facts.name||'');const n=q('name');if(n)n.textContent='@'+name;const w=q('workspace');if(w)w.textContent=String(facts.workspace||'');const i=q('initial');if(i)i.textContent=facts.initial||name.slice(0,1).toUpperCase();const s=q('sign-in');if(s)s.hidden=true;const session=q('session');if(session)session.hidden=false;return root;}global.cloudKotobaShell=Object.freeze({hydrateSession});})(globalThis);